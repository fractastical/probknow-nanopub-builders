# Moved

  This repo was a duplicate. The ProbKnow nanopub construction pipeline lives at:

  **[fractastical/probknow-ontology/pipeline](https://github.com/fractastical/probknow-ontology/tree/main/pipeline)**

  That's part of the full ontology repo (vocabulary + examples + queries + this pipeline), so it's the canonical home. This repo is archived and kept only so old links don't 404.
  